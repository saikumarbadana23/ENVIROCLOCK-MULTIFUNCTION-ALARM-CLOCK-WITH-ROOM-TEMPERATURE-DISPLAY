void delayus(unsigned int delay)
{	
	delay*=12;
	while(delay--);
}
void delayms(unsigned int delay)
{
delay*=12000;
while(delay--);
}
void delays(unsigned int delay)
{
delay*=12000000;
while(delay--);
}
